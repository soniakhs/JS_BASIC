<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Document</title>
    <script src="/dist.js"></script>
    <style>
        .box{
            width: 100px;
            height: 100px;
            transition: all 1s;
            background-color: #fff;

        }
        .box{
            width : 50px;
            height: 50px;
            background-color: #fff;
            border: 1px solid #ddd;
        }

        .red{
            background-color: #f00;
        }
        .green{
            background-color: rgb(8, 133, 8);
        }
    </style>
</head>
<body>
    <div id="app">
        <my-hello>
            <div slot="header">이 글은 헤더에 써집니다.,</div>
            <p>이 글은 섹션</p>
            <p>이 글은 섹션</p>
            <p>이 글은 섹션</p>
            <div slot="footer">이 글은 푸터에 써집니다.</div>
        </my-hello>
        <my-hello>양디</my-hello>
        <my-hello>소프트</my-hello>
        <div class="box" :style="colorSet">

        </div>
        <div v-if="items.length == 0">
            <h2>아이템을 불러오는 중입니다..</h2>
        </div>
        <div class="item" v-for="item in items" :key="item.id">{{item.name}}</div>
        <div :class="boxClassObj" class="box" :style="boxStyleObj">
            
        </div>
        <input type="checkbox" v-model="red">레드
        <input type="checkbox"  v-model="green">그린
    </div>
</body>
</html>